#include <stdio.h>

int main()
{
    int number1,number2;
    

    printf("Enter two integers:");
    scanf("%d %d",&number1,&number2);
    if(number1==number2)
    {
        printf("you are in if,Result:%d=%d",number1,number2);
    }
    else if(number1>number2)
    {
        printf("you are in else if,Result:%d =%d",number1,number2);
    }
    else
    {
        printf("you are in else,Result:%d=%d",number1,number2);
    }
    
    return 0;
}
